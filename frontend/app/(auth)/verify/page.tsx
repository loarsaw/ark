import VerificationPage from "@/components/Verify";
import React, { Suspense } from "react";

const Verify = () => {
  return (
    <Suspense>
      <VerificationPage />
    </Suspense>
  );
};

export default Verify;
